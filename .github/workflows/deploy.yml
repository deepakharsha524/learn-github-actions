name: deploy test using github action

on: [push, pull_request]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a sting
        run: echo "hello world"
      - name: multiline script
        run: |
          node -v
          npm -v